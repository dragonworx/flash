node_modules/
dist/
*.log

# Created at test setup, not committed — see tests/scan.test.ts and
# tests/dump-frame.test.ts. A broken symlink checked into git is a portability
# footgun (its target is meaningless on a fresh clone anyway).
tests/fixtures/sample/broken-link
